<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 인터넷 권한 (Gemini API 호출 필수) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- 카메라 권한 -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- 저장소 권한 (Android 12 이하) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" tools:replace="android:maxSdkVersion"/>
    <!-- 저장소 권한 (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!-- 알림 권한 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 정확한 알람 (알림 예약용) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- 진동 (알림음 대신 진동) -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- 포그라운드 서비스 (백그라운드 알림 유지) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Wake Lock (화면 꺼져있을 때도 알림) -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- 카메라 기능 선언 -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>

    <application
        android:label="FlowNote"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false"
        android:requestLegacyExternalStorage="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ══════════════════════════════════════════════════════
             카카오 로그인 리다이렉트 수신용 액티비티

             카카오톡이 없는 기기에서는 브라우저(커스텀 탭)로 로그인한다.
             로그인이 끝나면 카카오가 `kakao{네이티브앱키}://oauth` 로
             우리 앱을 다시 호출하는데, 그 주소를 받아줄 창구가 필요하다.
             이 액티비티가 없으면 브라우저 로그인 후 앱으로 돌아오지 못하고
             화면이 그대로 멈춘다.

             ${KAKAO_SCHEME} 는 build.gradle.kts 에서 채운다.
             (키를 저장소에 두지 않기 위해)
             ══════════════════════════════════════════════════════ -->
        <activity
            android:name="com.kakao.sdk.flutter.AuthCodeCustomTabsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="${KAKAO_SCHEME}" android:host="oauth"/>
            </intent-filter>
        </activity>

        <!-- flutter_local_notifications receiver -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- 카카오톡 설치 여부 확인 (Android 11+ 는 선언 없이는 못 본다).
             선언이 없으면 카카오톡이 깔려 있어도 항상 브라우저 로그인으로
             빠져서 사용자가 아이디/비밀번호를 직접 입력해야 한다. -->
        <package android:name="com.kakao.talk"/>
    </queries>
</manifest>












