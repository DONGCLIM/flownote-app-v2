# Firebase 설정 안내

프로젝트 ID: **`flownote-404ef`**
콘솔: https://console.firebase.google.com/project/flownote-404ef

---

## 먼저 — 이걸 꼭 해야 하나?

**아닙니다. 안 해도 앱은 다 됩니다.**

| 안 해도 되는 이유 | |
|---|---|
| 영수증 찍기·저장·정산 | 지금 됩니다 |
| 꽃 이름 자동완성 (247품목) | 지금 됩니다 |
| **꽃별 계절 시세** (인사이트 탭) | 지금 됩니다 — 앱 안에 4.5KB로 들어있음 |
| 이메일 로그인 | 지금 됩니다 |

**이 설정을 하면 늘어나는 것은 딱 한 줄입니다:**

```
📈 양재 경매가 11,827원/단 · 지난주보다 41% 올랐어요 · 8/21 경매
```

영수증 항목 아래에 붙는 이 한 줄이 전부입니다. 없으면 그 줄만 안 보이고,
나머지는 아무 영향 없습니다. **급한 일이 아닙니다.**

### 하기 싫으시면 — 안 하셔도 됩니다

터미널이 부담스러우면 **제가 대신 하겠습니다.** 방법 3개 중에 고르세요:

**① 제가 대신 한다 (가장 쉬움 · 5분 · 사장님은 클릭 3번)**
사장님이 하실 일은 이것뿐입니다:
1. Firebase 콘솔에서 요금제를 Blaze로 바꾸기 (카드 등록, 실제 청구는 0원)
2. 제 작업 계정을 프로젝트 멤버로 초대하기
3. "됐다"고 알려주기 → 나머지는 제가 다 합니다

**② 나중에 한다**
계절 시세를 먼저 써보시고, 경매 시세가 필요하다 싶으면 그때 하시면 됩니다.
데이터는 공판장에 계속 쌓이니까 나중에 켜도 2년치가 그대로 있습니다.

**③ 직접 한다**
아래 안내를 따라 15분. 막히면 화면 글자를 그대로 보내주세요.

---

## 지금 뭐가 안 되고 있나

| 기능 | 상태 | 막고 있는 것 |
|---|---|---|
| 영수증 저장·불러오기 | ✅ 됨 | — |
| 로그인 (이메일) | ✅ 됨 | — |
| 꽃 이름 자동완성 | ✅ 됨 | 앱 안에 들어있음 |
| **꽃별 계절 시세** (인사이트) | ✅ 됨 | 앱 안에 들어있음 (4.5KB) |
| **양재 경매 시세** (영수증 카드) | ❌ 안 보임 | **아래 A단계** |
| 구글 로그인 | ❌ 안 됨 | **아래 B단계** |
| 영수증 사진 업로드 | ⚠️ 확인 필요 | **아래 C단계** |

시세 줄은 데이터가 없으면 **스스로 사라지게** 만들어놨습니다.
그래서 지금 앱은 멀쩡하고, 그 한 줄만 안 보입니다.

---

## 준비 — 한 번만 하면 됩니다

컴퓨터에서 터미널(맥은 터미널, 윈도는 PowerShell)을 열고:

```bash
# 1. Firebase 명령 도구 설치 (Node.js 가 필요합니다. 없으면 nodejs.org 에서 설치)
npm install -g firebase-tools

# 2. 설치 확인 — 버전 숫자가 나오면 성공
firebase --version

# 3. 로그인 — 브라우저가 열리고 구글 계정을 고르면 됩니다
firebase login

# 4. 이 프로젝트 폴더로 이동 (flownote-app-v2 를 내려받은 위치)
cd 프로젝트_폴더_경로

# 5. 프로젝트가 제대로 연결됐는지 확인
firebase projects:list
```

5번에서 `flownote-404ef` 가 목록에 보이면 준비 끝입니다.
안 보이면 3번에서 다른 구글 계정으로 로그인한 것이니 `firebase logout` 후 다시 하세요.

---

## A단계 — 양재 경매 시세 켜기

경매 시세는 **서버가 하루 한 번** 공판장에서 받아와 요약해두고, 앱은 그
요약본(약 6KB)만 읽습니다. 그 서버를 올리는 작업입니다.

### A-1. 서비스키를 금고에 넣기

경매 API 서비스키를 코드에 적어두면 앱을 뜯어보는 사람이 볼 수 있습니다.
그래서 Google 금고(Secret Manager)에 넣고 서버만 꺼내 쓰게 합니다.

```bash
firebase functions:secrets:set FLOWER_API_KEY
```

실행하면 값을 물어봅니다. **서비스키를 붙여넣고 엔터**.

> 서비스키는 이 파일에 적지 않습니다. 이 저장소는 **공개(public)** 라서
> 여기 적으면 누구나 볼 수 있습니다. 키는 채팅으로 따로 전달받으세요.

> 처음이면 "Secret Manager API 를 켤까요?" 라고 물어볼 수 있습니다. `y` 하세요.
> 이때 **결제 계정(Blaze 요금제)** 연결을 요구할 수 있습니다. 아래 "돈 얘기" 참고.

### A-2. 서버 코드 준비

```bash
cd functions
npm install
cd ..
```

`functions/node_modules` 폴더가 생기면 성공입니다.

### A-3. 서버 올리기

```bash
firebase deploy --only functions
```

2~5분 걸립니다. 마지막에 이런 줄이 나옵니다:

```
✔  functions[updateFlowerPrices(asia-northeast3)] Successful create operation.
✔  functions[updateFlowerPricesNow(asia-northeast3)] Successful create operation.
Function URL (updateFlowerPricesNow(asia-northeast3)): https://asia-northeast3-flownote-404ef.cloudfunctions.net/updateFlowerPricesNow
```

**마지막 줄의 주소를 복사해두세요.** A-5에서 씁니다.

### A-4. 읽기 권한 열기

앱이 그 요약본을 읽을 수 있게 규칙을 올립니다.

```bash
firebase deploy --only firestore:rules
```

이 규칙이 하는 일: 로그인한 사용자는 시세를 **읽을 수만** 있고,
**쓰는 건 아무도 못 합니다**(서버는 이 규칙을 우회하므로 서버만 씁니다).

### A-5. 첫 데이터 채우기

서버는 매일 오후 5시 30분에 자동으로 도는데, **지금 당장 한 번** 돌려야
데이터가 생깁니다. A-3에서 복사한 주소를 브라우저 주소창에 붙여넣고 엔터:

```
https://asia-northeast3-flownote-404ef.cloudfunctions.net/updateFlowerPricesNow
```

이런 응답이 나오면 성공입니다:

```json
{"ok":true,"base":"2026-08-21","auctionDays":6,"itemCount":129,"bytes":6467}
```

- `auctionDays: 6` — 경매일 6일치를 찾았다는 뜻 (경매는 월·수·금만 열립니다)
- `itemCount: 129` — 품목 129개
- `bytes: 6467` — 요약본 크기. 원본 하루치가 185KB인데 6KB로 줄인 겁니다

### A-6. 앱에서 확인

앱을 완전히 껐다가 다시 켜고, 영수증 하나를 열어보세요.
꽃 이름 아래에 이런 줄이 생깁니다:

```
📈 양재 경매가 11,827원/단 · 지난주보다 41% 올랐어요 · 8/21 경매
```

**안 보이면** — 그 꽃이 최근 경매에 안 나온 철 지난 꽃일 수 있습니다.
장미·국화·거베라·백합으로 해보세요. 이 넷은 2년간 경매일 303일 전부
거래된 품목입니다.

---

## B단계 — 구글 로그인 켜기

1. 콘솔 열기 → https://console.firebase.google.com/project/flownote-404ef/authentication/providers
2. **Google** 줄을 클릭
3. 오른쪽 위 **사용 설정** 토글을 켜기
4. **프로젝트 지원 이메일** — 목록에서 사장님 이메일을 고르기 *(이거 안 고르면 저장이 안 됩니다)*
5. **저장**

이게 끝입니다. 앱 코드는 이미 준비돼 있어서 따로 배포할 필요 없습니다.

> 안드로이드에서 구글 로그인이 계속 실패하면 **SHA 지문**이 빠진 경우입니다.
> 그때는 알려주시면 등록 방법을 따로 안내하겠습니다.

---

## C단계 — 사진 업로드 확인

```bash
firebase deploy --only storage
```

Storage 를 아직 한 번도 안 만들었으면 이 명령이 실패합니다. 그럼:

1. https://console.firebase.google.com/project/flownote-404ef/storage
2. **시작하기** → 위치는 **asia-northeast3 (서울)** → 만들기
3. 그 다음 위 명령을 다시 실행

---

## 돈 얘기 — 얼마 나오나

Functions 를 쓰려면 **Blaze(종량제)** 요금제로 바꿔야 합니다.
"종량제"라니 무섭게 들리지만, 실제 사용량으로 계산해보면:

| 항목 | 우리 사용량 | 무료 한도 | 결과 |
|---|---|---|---|
| Functions 호출 | 하루 1번 = 월 **31번** | 월 200만 번 | 무료 |
| Functions 실행 시간 | 1회 약 25초 = 월 **13분** | 월 40,000분 | 무료 |
| 외부 통신 | 1회 약 2.6MB = 월 **80MB** | 월 5GB | 무료 |
| Firestore 쓰기 | 하루 1번 = 월 **31번** | 하루 20,000번 | 무료 |
| Firestore 읽기 | 사용자당 하루 최대 4번 | 하루 50,000번 | 무료 |

**월 0원입니다.** 무료 한도의 0.1% 도 안 씁니다.

시세를 **하루 한 번만** 부르도록 설계한 이유가 이겁니다. 영수증을 찍을
때마다 불렀다면 100장이면 18MB를 받아야 하고 응답도 매번 2초씩 기다려야
합니다. 지금은 영수증을 몇 장 찍어도 추가 통신이 **0**입니다.

혹시 걱정되시면 예산 알림을 걸어두세요:
https://console.firebase.google.com/project/flownote-404ef/usage/details
→ **예산 및 알림 설정** → 월 1,000원 정도로 걸어두면 넘을 때 메일이 옵니다.

---

## 잘 안 될 때

**`Error: Failed to get Firebase project flownote-404ef`**
→ 로그인한 구글 계정이 이 프로젝트 소유자가 아닙니다. `firebase logout` 후
   `firebase login` 으로 프로젝트를 만든 계정으로 다시 들어가세요.

**`Error: HTTP Error: 403, Billing account for project is not found`**
→ Blaze 요금제로 안 바뀐 상태입니다.
   https://console.firebase.google.com/project/flownote-404ef/usage/details
   에서 **요금제 수정** → Blaze 선택.

**`npm install` 이 오류를 뱉는다**
→ Node.js 버전 문제일 수 있습니다. `node --version` 이 **v20 이상**인지
   확인하세요. 낮으면 nodejs.org 에서 최신 LTS 를 설치하세요.

**A-5 주소를 열었는데 `{"ok":false, ...}` 가 나온다**
→ 공판장 서버가 일시적으로 죽었을 수 있습니다. 5분 뒤 다시 열어보세요.
   계속 실패하면 `firebase functions:log` 결과를 보여주세요.

**A-5는 성공했는데 앱에 안 보인다**
→ ① 앱을 완전히 종료했다 다시 켰는지 (백그라운드 아님)
   ② 로그인 상태인지 (규칙이 로그인한 사용자만 읽게 합니다)
   ③ 철 지난 꽃이 아닌지 — 장미로 시도

---

## 하고 나면 이렇게 돌아갑니다

```
매일 오후 5시 30분 (한국시간)
        │
        ▼
  서버(Cloud Functions)가 공판장 API 호출
  ├─ 오늘부터 거꾸로 훑어 경매일 6일치를 찾는다
  │  (경매는 월·수·금만 열려서 14번쯤 호출하게 됩니다)
  ├─ 품목별로 물량가중 평균가를 낸다
  │  (등급 섞임은 실측 결과 영향 1~11%로 미미해서 단순 가중평균)
  ├─ 최근 3경매일 vs 이전 3경매일 로 지난주 대비 % 계산
  └─ 요약본 하나(약 6KB)를 Firestore 에 저장
        │
        ▼
  앱은 하루 한 번 그 6KB 만 읽는다
  ├─ 읽은 건 폰에 저장해두고 6시간 안엔 다시 안 받는다
  └─ 영수증을 몇 장 찍어도 추가 통신 0
```
